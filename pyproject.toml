[project]
name = "skillora-fastapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
license = { text = "GPL-3.0-only" }
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
]
